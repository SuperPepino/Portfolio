---
import "../styles/global.css";
const currentPath = Astro.url.pathname;
---
<header class="w-full bg-[#161622] backdrop-blur border-b">
    <div class="max-w-[1400px] w-full mx-auto flex justify-between items-center px-24 max-sm:px-5 max-sm:py-10 ">
        <a href="/" class="flex items-center gap-x-4">
            <img
            src="/portfolioicongray.svg"
            width={160}
            height={160}
            alt="favicon"
            class = "rounded-2xl aspect-thumbnail object-cover drop-shadow-md transition-transform duration-300 hover:rotate-6 hover:scale-110"
        />
        </a>
        
        <ul class="flex gap-x-9">
            <li><a href="/" class={`nav-link font-bold ${currentPath === "/" ? "active" : ""}`}>Home</a></li>
            <li><a href="/projects" class={`nav-link font-bold ${currentPath.startsWith("/projects") ? "active" : ""}`}>Projects</a></li>
            <li><a href="/about" class={`nav-link font-bold ${currentPath.startsWith("/about") ? "active" : ""}`}>About me</a></li>
        </ul>
    </div>
</header>